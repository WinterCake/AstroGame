/** Wrapper extension — logique dans lib/spy-core.js (généré depuis shared/). */
const S = AstrogameSpyCore;
const V = AstrogameVerdict;

const filterSpyReports = S.filterSpyReports;
const isSansDefense = S.isSansDefense;
const isGrosButinSansDefense = S.isGrosButinSansDefense;
const summarizeSpyPayload = S.summarizeSpyPayload;
const parseSpyReportsHtml = S.parseSpyReportsHtml;
const mergeSpyReports = S.mergeSpyReports;
const buildSpyPayload = S.buildSpyPayload;
const isReportToday = S.isReportToday;
const detectMaxSpyPage = S.detectMaxSpyPage;
const formatReportDate = S.formatSpyReportDate;
const verdictClass = V.verdictClass;
